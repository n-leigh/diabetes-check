services:
  - type: web
    name: diabeates
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python wsgi.py
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
